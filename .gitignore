node_modules/
.env
.env.local
artifacts/
cache/
typechain-types/
coverage/
coverage.json
web/.next/
web/node_modules/
web/out/
*.log
.DS_Store

# Runtime artifacts (regenerated by deploy / the agent).
# Note: web/feed.json IS committed so the static dashboard shows data on first deploy.
deployments.json
data/
